<%- include('partials/header', {user: user, isAdmin: typeof isAdmin !== 'undefined' ? isAdmin : false}) %>
  <div class="content-container">
    <div class="page-hero">
      <h1>👥 Staff Dashboard</h1>
      <p class="intro-text">Quick overview for staff members</p>
    </div>

    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-icon orange">📋</div>
        <div>
          <h3 style="margin:0;font-size:0.75rem;text-transform:uppercase;letter-spacing:0.05em;color:var(--text-muted)">Total Cases</h3>
          <div class="value" style="font-size:1.75rem;font-weight:700"><%= stats && stats.warnings ? stats.warnings : '0' %></div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon blue">📝</div>
        <div>
          <h3 style="margin:0;font-size:0.75rem;text-transform:uppercase;letter-spacing:0.05em;color:var(--text-muted)">Command Logs</h3>
          <div class="value" style="font-size:1.75rem;font-weight:700"><%= stats && stats.commands ? stats.commands : '0' %></div>
        </div>
      </div>
    </div>

    <div class="grid" style="margin-top:24px">
      <div class="card">
        <h3>📋 Cases</h3>
        <p>View all warnings, bans, and fines issued on the server.</p>
        <a class="btn btn-secondary" href="/cases" style="margin-top:16px">View Cases</a>
      </div>

      <div class="card">
        <h3>📝 Commands</h3>
        <p>Browse the command log to see recent staff actions.</p>
        <a class="btn btn-secondary" href="/commands" style="margin-top:16px">View Logs</a>
      </div>

      <div class="card">
        <h3>🔗 Accounts</h3>
        <p>View linked Discord and Minecraft accounts.</p>
        <a class="btn btn-secondary" href="/cases" style="margin-top:16px">View Linked</a>
      </div>
    </div>
  </div>
<%- include('partials/footer') %>
