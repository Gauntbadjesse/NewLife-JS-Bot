<%- include('partials/header', {user: user, isAdmin: typeof isAdmin !== 'undefined' ? isAdmin : false}) %>
  <div class="content-container">
    <div class="page-hero">
      <h1>📊 Dashboard</h1>
      <p class="intro-text">Overview of moderation data and server activity</p>
    </div>

    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-icon orange">⚠️</div>
        <div>
          <h3 style="margin:0;font-size:0.75rem;text-transform:uppercase;letter-spacing:0.05em;color:var(--text-muted)">Warnings</h3>
          <div class="value" style="font-size:1.75rem;font-weight:700"><%= stats.warnings || 0 %></div>
          <div class="sub"><%= stats.activeWarnings || 0 %> active</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon red">🔨</div>
        <div>
          <h3 style="margin:0;font-size:0.75rem;text-transform:uppercase;letter-spacing:0.05em;color:var(--text-muted)">Bans</h3>
          <div class="value" style="font-size:1.75rem;font-weight:700"><%= stats.bans || 0 %></div>
          <div class="sub"><%= stats.activeBans || 0 %> active</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon blue">📝</div>
        <div>
          <h3 style="margin:0;font-size:0.75rem;text-transform:uppercase;letter-spacing:0.05em;color:var(--text-muted)">Commands</h3>
          <div class="value" style="font-size:1.75rem;font-weight:700"><%= stats.commands || 0 %></div>
          <div class="sub">logged actions</div>
        </div>
      </div>
    </div>

    <div class="card">
      <h3>Quick Links</h3>
      <div style="display:flex;gap:12px;flex-wrap:wrap;margin-top:16px">
        <a href="/cases" class="btn btn-secondary">View All Cases</a>
        <a href="/commands" class="btn btn-secondary">Command History</a>
        <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
          <a href="/admin" class="btn btn-primary">Admin Console</a>
        <% } %>
      </div>
    </div>
  </div>
<%- include('partials/footer') %>